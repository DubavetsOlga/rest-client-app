export const { HISTORY, LOGIN, REGISTRATION, REST, VARIABLES } = {
  HISTORY: '/history',
  LOGIN: '/login',
  REGISTRATION: '/registration',
  REST: '/client',
  VARIABLES: '/variables',
} as const;
